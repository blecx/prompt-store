# Scripts

## Available scripts

- `build_report.py` — combines the split report into `dist/report-manuscript.md`
- `check_placeholders.py` — identifies unresolved placeholders such as `<...>`
- `create_section.py` — creates a new report section file from a small template
- `export_manifest.py` — exports a manifest of tracked report files
